---
apiVersion: app/v1
kind: front-end
metadata:
  name: front-end
spec:
  replicas: 3
  selector:
    matchLabels:
      app: front-end
  template:
    metadata:
      labels:
        app: front-end
    spec:
      containers:
      - name: front-end-container
        image: yashparmar04/testing:latest
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: testing-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
  type: NodePort
